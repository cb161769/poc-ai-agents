version: "3.9"

services:
  neo4j:
    image: neo4j:5.12.0-community
    container_name: poc-neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: "neo4j/test_password_local"
      NEO4J_server_memory_pagecache_size: "256m"
      NEO4J_server_memory_heap_initial__size: "256m"
      NEO4J_server_memory_heap_max__size: "512m"
    volumes:
      - neo4j_data:/data
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p test_password_local 'RETURN 1' || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 20s
    networks:
      - poc-net

  graph-seeder:
    image: neo4j:5.12.0-community
    container_name: poc-graph-seeder
    depends_on:
      neo4j:
        condition: service_healthy
    volumes:
      - ./seed:/seed:ro
    entrypoint: ["/bin/sh", "-c"]
    command: >
      "cypher-shell -a bolt://neo4j:7687 -u neo4j -p test_password_local -f /seed/seed.cypher &&
       echo '[graph-seeder] seed aplicado (fallback estatico; correr prompts/sync_graph_from_azure_devops.md para datos reales)'"
    networks:
      - poc-net

  sonarqube:
    image: sonarqube:10.6-community
    container_name: poc-sonarqube
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      SONAR_JDBC_URL: "jdbc:h2:tcp://localhost/sonar"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_extensions:/opt/sonarqube/extensions
      - sonarqube_logs:/opt/sonarqube/logs
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 60s
    networks:
      - poc-net

  sonar-scanner:
    image: sonarsource/sonar-scanner-cli:latest
    container_name: poc-sonar-scanner
    depends_on:
      sonarqube:
        condition: service_healthy
    volumes:
      - ./sample-repo:/usr/src:ro
      - ./scripts:/scripts:ro
      - ./.env:/scripts/.env:ro
      - sonar_bootstrap_state:/state
    entrypoint: ["/bin/sh", "/scripts/bootstrap_sonar.sh"]
    environment:
      SONAR_HOST_URL: "http://sonarqube:9000"
    networks:
      - poc-net

  qdrant:
    image: qdrant/qdrant:latest
    container_name: poc-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:6333/readyz || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    networks:
      - poc-net

  rag-indexer:
    build:
      context: .
      dockerfile: Dockerfile.firewall
    container_name: poc-rag-indexer
    depends_on:
      qdrant:
        condition: service_healthy
    volumes:
      - ./sample-repo:/app/sample-repo:ro
      - ./scripts:/app/scripts:ro
      - ./cache:/app/cache
      - ./.env:/app/.env:ro
    entrypoint: ["python", "/app/scripts/index_rag_corpus.py"]
    env_file:
      - .env
    environment:
      QDRANT_URL: "http://qdrant:6333"
    networks:
      - poc-net

  ollama:
    image: ollama/ollama:latest
    container_name: poc-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:11434/api/tags || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s
    networks:
      - poc-net

  # Init container de un solo uso: descarga el modelo de tool-calling que
  # necesitan el agente juez / coding_agent.py contra el servidor "ollama" ya
  # levantado, para que "docker exec poc-ollama ollama pull llama3.1" ya no
  # sea un paso manual del README (§11). Se conecta via OLLAMA_HOST, no baja
  # nada localmente -- el modelo queda en el volumen ollama_models real.
  ollama-pull:
    image: ollama/ollama:latest
    container_name: poc-ollama-pull
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: "ollama:11434"
    entrypoint: ["ollama", "pull", "${OLLAMA_MODEL:-llama3.1}"]
    networks:
      - poc-net

  prefect-server:
    image: prefecthq/prefect:2-latest
    container_name: poc-prefect-server
    command: ["prefect", "server", "start", "--host", "0.0.0.0"]
    ports:
      - "4200:4200"
    volumes:
      - prefect_data:/root/.prefect
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:4200/api/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s
    networks:
      - poc-net

  falco:
    image: falcosecurity/falco:latest
    container_name: poc-falco
    privileged: true
    pid: host
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
      - /proc:/host/proc:ro
      - /etc:/host/etc:ro
      - /boot:/boot:ro
      - /lib/modules:/host/lib/modules:ro
      - /usr:/host/usr:ro
      - ./falco/custom_rules.yaml:/etc/falco/rules.d/custom_rules.yaml:ro
      - ./logs:/var/log/falco
    command:
      - /usr/bin/falco
      - --modern-bpf
      - -o
      - json_output=true
      - -o
      - file_output.enabled=true
      - -o
      - file_output.filename=/var/log/falco/falco_alerts.jsonl
      - -o
      - file_output.keep_alive=true
    networks:
      - poc-net

  ai-firewall:
    build:
      context: .
      dockerfile: Dockerfile.firewall
    container_name: poc-ai-firewall
    restart: unless-stopped
    depends_on:
      neo4j:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
      - ./cache:/app/cache
      - ./.env:/app/.env:ro
    env_file:
      - .env
    environment:
      NEO4J_URI: "bolt://neo4j:7687"
      QDRANT_URL: "http://qdrant:6333"
      SONAR_URL: "http://sonarqube:9000"
    command: ["uvicorn", "firewall_proxy:app", "--host", "0.0.0.0", "--port", "8080"]
    networks:
      - poc-net

networks:
  poc-net:
    driver: bridge

volumes:
  neo4j_data:
  sonarqube_data:
  sonarqube_extensions:
  sonarqube_logs:
  sonar_bootstrap_state:
  ollama_models:
  prefect_data:
